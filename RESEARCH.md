Building Bennett Studio — an open-source database workspace for modern developers. Install, manage, query, and share PostgreSQL, MySQL, MariaDB, SQLite locally with zero-config Docker provisioning. Secure tunnel sharing, schema-aware permissions, multi-client architecture (Desktop, Web, CLI, VS Code). Rust + Tauri + React. Previously built Accella (wellness platform).

The database workspace for modern developers. Install, query, and share local databases with zero-config Docker provisioning and secure tunnel sharing.


Existing examples:
TablePlus — paid, popular
Beekeeper Studio — open source, cross-platform
DBeaver — open source, heavy
PgAdmin — PostgreSQL only, clunky
phpMyAdmin — web-based, old